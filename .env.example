# Browser-safe variables only. Never put trusted credentials here.
PUBLIC_SUPABASE_URL=http://127.0.0.1:54331
PUBLIC_SUPABASE_PUBLISHABLE_KEY=replace-with-local-supabase-anon-key
PUBLIC_SITE_URL=http://127.0.0.1:5173
# Optional validated browser-safe projection (version/brand/locale/quote presentation only).
# Never put credentials, trusted environment names/values, secret references, roles, state,
# prices/totals, or private operational configuration here.
PUBLIC_CLIENT_CONFIG_JSON=
