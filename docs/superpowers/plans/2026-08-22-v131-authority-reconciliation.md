# v1.3.1 Authority Reconciliation Plan

> Execute this plan in the existing repository. Do not restart P0–P14 or replace the implementation with the authority backup.

## 1. Establish evidence and state

- Capture the clean `2d76e12` baseline and current v1.1 validation evidence.
- Compare all current and v1.3.1 phase test IDs and produce the 156 → 229 registry delta.
- Classify each appended requirement and each changed architecture/toolchain/security/recovery contract in the reconciliation matrix.
- Move persistent state into the v1.3.1 loop schema without claiming terminal readiness.

## 2. Repair implementation truth

- Convert Wrangler, preview, Playwright and provisioning paths to Workers + Static Assets with a frozen compatibility date.
- Add the exact dependency/toolchain proof, project-local Supabase CLI, Zod boundary support, and approved shadcn-svelte ownership configuration without introducing a second UI/state/form/date stack.
- Add a forward SQL migration for attention/pause orthogonality, phone normalisation, protected mutation fields, trusted-function privilege/AAL2 enforcement, outbound logical-message attempts/uncertainty/reconciliation, hard-bounce corrective-task idempotency, analytics security-invoker guarantees, and audit evidence.
- Extend backup/restore manifests and tests for Auth identity/profile/role/status reconstruction and explicit password/MFA recovery expectations.
- Add focused v1.3.1 gates and preserve every existing mandatory test and assertion.

## 3. Repair normative documentation

- Add the canonical money, metrics, privacy and recovery contracts plus the toolchain proof.
- Update architecture, security, deployment, operations, client deployment, README, pilot readiness and scripts so none still claim Pages or unsupported recovery.
- Keep post-v1 scope explicitly deferred.

## 4. Import and regenerate authorities

- Copy the preserved v1.3.1 controller, roadmap, dependency baseline, architecture blueprint, phase authorities, release artifacts and manifest into Git only after implementation truth matches them.
- Regenerate `docs/REQUIREMENTS_COVERAGE.md`, authority hashes and v1.3.1 state/hand-off evidence.

## 5. Validate and close

- Run narrow focused tests, migration reset/upgrade, complete quality/security/database/browser/build gates, explicit Won/Lost journeys, authority drift/hash checks, and release-state checks.
- Repair failures in place and repeat the relevant gates.
- Inspect the staged diff, create one clean local checkpoint commit on `main`, and verify the working tree is clean. Do not push.
