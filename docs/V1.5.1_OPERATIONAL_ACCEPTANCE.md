# Zephyr CRM v1.5.1 Operational Acceptance

Date: 2026-08-30
Baseline: `main` at `298e26b6826d7eaec5192c2a7a452cc7d337c59b`

This evidence covers OA-01 through OA-05 for the operational-polish branch. All browser and database acceptance runs used the local Supabase target only. Banking values in fixtures are disposable test data; no real banking details, credentials, or provider secrets are included.

## Acceptance findings

| Finding | Evidence | Result |
| --- | --- | --- |
| OA-01 — desktop Quote preview | `bun run test:v151:operational-acceptance` measures the preview at 1280px, asserts at least 420px of preview width, asserts no document overflow, and checks desktop numeric no-wrap plus mobile numeric/terms wrapping. | PASS |
| OA-02 — logo and default branding safety | The same acceptance run renders the default monogram fallback in the browser. Focused unit tests cover an empty client logo path, no empty-asset fetch, and PDF fallback for unsupported/empty assets. The v1.5.1 migration and seed remove the broken `/favicon.svg` default while preserving explicitly configured non-default assets. | PASS |
| OA-03 — Quote creation entry point | The acceptance run asserts the canonical `Open Quote Builder` link beside the unchanged quick custom form, including the lead-scoped `/quotes/new?lead_id=...` target, catalogue wording, line-item input, and `Create quote` action. | PASS |
| OA-04 — trusted Quote defaults and banking configuration | The acceptance run proves an AAL2 Owner can open Operations, receives validation without bank-detail echo on invalid input, saves disposable settings, and sees server-owned tax/terms/validity defaults in the canonical Builder. The route remains Owner/Admin-only and writes through the existing trusted `set_app_setting` boundary. `bun run auth:readiness` and `bun run db:security` pass the surrounding MFA and role-boundary contracts. | PASS |
| OA-05 — PDF Product-code wrapping | Focused PDF tests prove hyphen-boundary wrapping, full-width fitting for `OPS-ROOF-INSPECT-01`, preservation of unbroken codes, deterministic output, and zero layout overflow. | PASS |

## Validation record

Fresh local validation after `bun run db:reset`:

- `bun run test:v151:operational-acceptance` — 4 passed.
- `bun run test:e2e -- tests/e2e/domain/p17-sales-fulfilment.e2e.ts tests/e2e/domain/p19-fulfilment.e2e.ts` — 2 passed with the normal two-worker run.
- `bun run test:p7:quotes` — passed.
- `bun run test:p8:documents` — passed, 15 focused integration tests.
- `bun scripts/test-p24-quote-builder.mjs` — passed.
- Focused unit suite for defaults, client configuration, quote document assets, and PDF v2 — 23 passed.
- `bun run test` — 102 unit tests and 7 baseline browser tests passed.
- `bun run format:check`, `bun run lint`, `bun run check`, and `bun run build` — passed.
- `bun run auth:readiness`, `bun run db:test`, `bun run db:types:check`, and `bun run db:security` — passed.
- `git diff --check` — passed.

The local SendPulse browser fixture now uses UUID provider message IDs so concurrent P17/P19 disposable sends cannot collide with the production uniqueness constraint. This is test-fixture-only; the SendPulse adapter and production boundary are unchanged.

## Scope confirmation

The Product → Quote → PDF → Client → Fulfilment architecture, lifecycle state machines, QuoteItem snapshot semantics, document immutability/hash rules, SendPulse boundary, and Fulfilment handoff are unchanged. No dependency, queue, Redis, service, payment, public portal, deployment, production, or v1.6 work is included.
